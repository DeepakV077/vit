# Copy this file to .env and fill the values. NEVER commit your real API keys.
# Example environment variables for the Gemini / Google Generative AI proxy
GEMINI_API_KEY=your_real_key_here
# Full endpoint URL to send requests to (the proxy will forward the request body as-is)
# Example (Google Generative API): https://generativelanguage.googleapis.com/v1beta2/models/your-model:generateText
GEMINI_API_URL=
# Optional: model identifier if your client or proxy needs it
GEMINI_MODEL=alloy:20230515
# Optional: a lightweight URL the proxy can use for an auth check. If set,
# the dev proxy will perform a HEAD request to this URL using the API key and
# include a small `healthCheck` object on /make-server-86c3a706/health. Do not
# set this to a URL that will echo or leak your key.
GEMINI_HEALTH_CHECK_URL=
